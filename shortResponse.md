# Short Response — LG 7.0: Responsive Units

Answer each question in 2–3 sentences. 

---

## Question 1 — Describe

What is the difference between a static unit like `px` and a responsive unit like `%` or `vh`?

Describe what makes a unit responsive and why that matters when building a website.

The difference between a static unit px and a responsive unit vh is that 'px' is great for small deatails that shows never change size like icons. While the responsive unit 'vh' chnages size of elements based on user screen size. What makes a unit repsonsive is that fact it changes based on the screen, it matters when buliding a website becuase it hepls with the foundation.










---

## Question 2 — Explain

Look at these two CSS rules:

```css
.image {
  width: 400px;
}

.image {
  width: 50%;
}
```

Explain what happens to the image on a small screen with each rule. Why does one behave better than the other?

with rule two it would take up most of the page, while with rule 1 it wouldnt 





